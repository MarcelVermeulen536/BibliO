{
  "name": "bibliotheque-electron-angular-prisma",
  "version": "1.0.0",
  "description": "BiblioTheque - gestionnaire de bibliotheque personnelle (Electron + Angular + Prisma + SQLite)",
  "main": "dist/main.js",
  "scripts": {
    "postinstall": "prisma generate",
    "build:main": "tsc -p tsconfig.json",
    "build:angular": "cd renderer/app && ng build",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "db:deploy": "prisma migrate deploy",
    "seed": "node dist/seed.js",
    "setup": "npm run prisma:migrate && npm run build:main && npm run seed",
    "start": "npm run build:main && npm run db:deploy && npm run seed && npm run build:angular && electron ."
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "prisma": "^6.2.1",
    "typescript": "^5.6.3"
  }
}
